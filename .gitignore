# Reference files — too large for GitHub
# Download instructions in README.md
Ref/*.fna
Ref/*.fna.fai
Ref/*.fna.gz
Ref/*.gff
Ref/*.gff.gz

# STAR genome indices
Ref/STAR_index*/

# STAR temporary working directory
_STARtmp/

# Simulation outputs (generated by the scripts)
*/simreads/
*/bam/
*/logs/
*.fastq
*.bam
*.bai

# Claude Code local settings and project memory (stored in ~/.claude/ globally)
.claude/

# Run logs (full SLURM output — large, not versioned)
mappability_all_*.out

# Third-party reference / transparency files (large or not redistributable)
HLA_MAFFT/TransparentScience_HLA_Li.txt

# Kallisto run_info.json — contains cluster username and scratch paths
mappability/kallisto_results/*/run_info.json

# Result directories — uncomment once TSVs are copied out
# results*/
